<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/bg_bottom_bar"
    android:elevation="8dp"
    android:padding="16dp"
    android:textAlignment="viewStart"> <!-- La elevación es clave para la sombra -->

    <!-- Stepper de Cantidad (reutilizando el diseño anterior) -->
    <LinearLayout
        android:id="@+id/layoutStepper"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/bg_stepper"
        android:gravity="center"
        android:orientation="horizontal"
        android:padding="4dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <Button
            android:id="@+id/btnMenos"
            style="@style/Widget.AppCompat.Button.Borderless"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:text="-"
            android:textColor="#631BE4"
            android:textSize="20sp" />

        <TextView
            android:id="@+id/tvCantidad"
            android:layout_width="40dp"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:text="1"
            android:textColor="@color/black"
            android:textSize="18sp"
            android:textStyle="bold" />

        <Button
            android:id="@+id/btnMas"
            style="@style/Widget.AppCompat.Button.Borderless"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:text="+"
            android:textColor="#631BE4"
            android:textSize="20sp" />
    </LinearLayout>

    <!-- Botón de Agregar -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnAgregarAlCarrito"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:backgroundTint="#631BE4"
        android:paddingVertical="14dp"
        android:text="Agregar"
        android:textAllCaps="false"
        app:cornerRadius="8dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@id/layoutStepper"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- Texto de "Total" que se superpone al botón -->
    <TextView
        android:id="@+id/tvPrecioTotal"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:elevation="2dp"
        android:text="$ 10.00"
        android:textColor="@color/white"
        android:textStyle="bold"
        app:layout_constraintBottom_toBottomOf="@id/btnAgregarAlCarrito"
        app:layout_constraintEnd_toEndOf="@id/btnAgregarAlCarrito"
        app:layout_constraintHorizontal_bias="0.9"
        app:layout_constraintStart_toStartOf="@id/btnAgregarAlCarrito"
        app:layout_constraintTop_toTopOf="@id/btnAgregarAlCarrito" />

</androidx.constraintlayout.widget.ConstraintLayout>